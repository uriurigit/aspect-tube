import { getRouteName } from '@site-information/modules/site-info-modules';

export default class AspectTubeRouteInfo {
    public static readonly root = getRouteName(AspectTubeRouteInfo);
    public static readonly top = 'top';
}
